# TASKLIST · R54 — SMART INPUT למאזן (יום 1: שכבת לוגיקה טהורה + טסטים)

> ממש את ה-spec "R45 — SMART INPUT למאזן". (השם R45 כבר תפוס בהיסטוריה
> — סבב photoreal ROOM 3D — לכן הסבב הזה ממוספר R54 ברצף.)

**Date:** 2026-05-19 · tsc ✅ · lint ✅ (0 err) · test ✅ 75/75 · no new deps / no migration.

Owner: קלט חכם להזנת ~200 מעטפות ב-5 דקות — קול (Web Speech he-IL)
+ סריקת מעטפות (OpenAI Vision דרך Edge). ה-spec מחייב סדר עבודה
מדורג: **יום 1** = 3 ספריות טהורות + טסטים ירוקים, ורק אז להמשיך.

## Done (deterministic, verifiable — יום 1 בלבד)

1. **`lib/hebrewNumbers.ts`** — מילות-מספר עברית → מספר שלם. טהור,
   איזומורפי, אפס תלויות. יחידות (זכר/נקבה), עשרות, מאות
   (מאה/מאתיים/X מאות), אלפים (אלף/אלפיים/שלושת אלפים), המחבר ו־,
   ומחרוזות ספרות. `null` כשאין מספר.
2. **`lib/voiceParser.ts`** — תמליל דיבור → `ParsedEntry[]`
   (`{name, amount, rawText}`). פיצול על מפרידים מדוברים
   (ואז / וגם / אחר כך / פסיק / שורה) **וגם** מכונת-מצב per-chunk
   שמפצלת רצף בלי פסיקים ("אבי 500 שירה 300 יוסי 400" → 3). רק
   רשומה עם סכום > 0 נוצרת; הכול עובר downstream דרך אישור.
3. **`lib/nameMatcher.ts`** — התאמת שם עברי מטושטשת (Levenshtein +
   נרמול: אותיות סופיות, ניקוד, ה־ פתיחה). מחזיר עד 3 הצעות מדורגות
   עם סיבה, סינון score>0.3.
4. **טסטים (Day-1 gate):**
   - `tests/hebrewNumbers.test.ts` — 36 מקרים (ספרות, יחידות,
     עשרות+ו, מאות, אלפים, לא-מספר→null, `isNumberWord`).
   - `tests/voiceParser.test.ts` — 19 מקרים (יחיד, מפרידים מפורשים,
     רצף ללא מפריד, סכומים בעברית, שם רב-מילים, מילות-מילוי,
     אפס/ריק, שם-בלבד).
   - `tests/nameMatcher.test.ts` — 13 מקרים (מדויק, שם-פרטי, prefix,
     Levenshtein קרוב/2, סינון סף, ה־ פתיחה, אות סופית+ניקוד, ריק,
     דירוג + תקרת-3).

ה-spec ביקש 20/15/10 מקרים — סופקו 36/19/13 (66 חדשים, 75 בסך הכול).

## Honest deferral — ימים 2-3 (חומרה/שירות-חיצוני/אישור-מכשיר)

לא בוצעו ומסומנים במפורש כי הם דורשים מה שאי-אפשר לאמת headless:

- **יום 2:** `VoiceCapture` (Web Speech API + getUserMedia — דורש
  מיקרופון ומכשיר), זרימת Confirmation, אינטגרציית מאזן.
- **יום 3:** Edge endpoint ל-OCR (OpenAI Vision gpt-4o — דורש מפתח
  סודי בצד-שרת, אסור ב-bundle), `EnvelopeScan` (מצלמה), `SmartInputFAB`.

הסיבה: ה-spec מחייב קבלה על מכשיר אמיתי + פרטיות (מפתח OpenAI לא
ב-client, תמליל נשאר client-side). שכבת הלוגיקה הטהורה היא החלק
הניתן-לאימות-דטרמיניסטי, והיא הושלמה ונבדקה לפי השער של ה-spec עצמו.

## Verification

- ✓ `npx vitest run` → 5 files / 75 passed (66 חדשים).
- ✓ `npx tsc --noEmit` נקי. ✓ `npm run lint` 0 errors
  (6 warnings קודמות, לא בקבצים החדשים).
- ✓ אפס תלויות חדשות · אפס מיגרציה · 3 הספריות pure/isomorphic.
- ⏳ ימים 2-3 (קול/מצלמה/OCR) — owner-side, מצריך חומרה ומפתח שרת.
